gem "public_suffix", ">= 5.1.1", "< 7.0"  # 允许 5.x 或 6.x
# 添加全局源（必须）
source "https://rubygems.org"

# 添加 jekyll-paginate 插件（关键缺失）
gem "jekyll-paginate"

# 如果使用 GitHub Pages，推荐直接继承其依赖
gem "github-pages", group: :jekyll_plugins